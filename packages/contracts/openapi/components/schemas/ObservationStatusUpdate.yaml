type: object
required: [status]
properties:
  status:
    type: string
    enum: [preliminary, final, amended, cancelled, entered-in-error]
    description: |
      Next state per the observation state machine
      (`preliminary → final → amended`; non-terminal → `cancelled` or
      `entered-in-error`).
