type: object
properties:
  date:
    type: string
    format: date-time
  status:
    type: string
    enum: [booked, arrived, fulfilled, cancelled, noshow]
    description: |
      `booked → arrived → fulfilled`; `booked → cancelled` or `noshow`.
  reason:
    type: string
    maxLength: 500
